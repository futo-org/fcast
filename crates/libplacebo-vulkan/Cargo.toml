[package]
name = "libplacebo-vulkan"
version = "0.1.0"
edition = "2024"
license.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
libplacebo = { path = "../libplacebo", features = ["vulkan"] }
