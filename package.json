{
  "name": "fcast",
  "version": "1.0.0",
  "scripts": {
    "deploy-site": "wrangler pages deploy --directory ./website --branch main --project-name fcast"
  },
  "dependencies": {
    "wrangler": "^3.88.0"
  }
}
