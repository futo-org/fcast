[workspace]
members = [
    "xtask",
    "sdk/sender/fcast-sender-sdk",
    "sdk/sender/flutter-plugin/rust",
    "senders/terminal",
    "crates/fcast-protocol",
    "crates/mirroring-core",
    "crates/sabrump",
    "senders/desktop",
    "senders/android",
    "tools/fast",
    "crates/file-server",
    "senders/android",
    "crates/receiver-core",
    "crates/receiver-ui",
    "receivers/desktop",
    "receivers/experimental/android",
    "receivers/experimental/fhs",
    "crates/app-updater",
    "crates/google-cast-protocol",
    "crates/receiver-resources",
    "crates/libplacebo-sys",
    "crates/libplacebo",
    "crates/libplacebo-vulkan",
    "crates/egl-sys",
    "crates/fiatlux-sys",
    "crates/fiatlux",
    "crates/inhibit-screensaver",
    "crates/fcastplaybin",
    "crates/fcast-gst-elements",
    "crates/fcasttest",
    "crates/fcast-runtime",
    "crates/fcast-video",
    "fuzz",
    "sdk/sender/examples/image-viewer",
    "crates/apple-fairplay",
]
# Plain `cargo build`/`test`/`check` at the root builds only these; `-p` and
# `--workspace` still reach everything, and every crate below is still a member.
#
# Excluded: the image-viewer example (slint + femtovg for an SDK example — dead
# weight in every sweep), the android crates (need the NDK), fhs + fiatlux (need
# the system libfiatlux-client this host lacks), and the sender side — the
# senders, the sender SDK and its flutter plugin, plus the crates only they use
# (mirroring-core, file-server, tools/fast). Work here is receiver work; the
# sender tree is one `cargo xtask test-sender` away. Shared crates
# (fcast-protocol, sabrump, google-cast-protocol) stay: the receiver uses them.
default-members = [
    "xtask",
    "crates/fcast-protocol",
    "crates/sabrump",
    "crates/receiver-core",
    "crates/receiver-ui",
    "receivers/desktop",
    "crates/app-updater",
    "crates/google-cast-protocol",
    "crates/receiver-resources",
    "crates/libplacebo-sys",
    "crates/libplacebo",
    "crates/libplacebo-vulkan",
    "crates/egl-sys",
    "crates/inhibit-screensaver",
    "crates/fcastplaybin",
    "crates/fcast-gst-elements",
    "crates/fcasttest",
    "crates/fcast-runtime",
    "crates/fcast-video",
    "fuzz",
    "crates/apple-fairplay",
]
resolver = "2"

[workspace.package]
license = "MIT"

[workspace.dependencies]
tokio = { version = "1.52", features = [ "full" ] }
serde = { version = "1.0", features = [ "derive" ] }
gst = { package = "gstreamer", version = "0.25" }
gst-app = { package = "gstreamer-app", version = "0.25", features = [ "v1_24" ] }
gst-video = { package = "gstreamer-video", version = "0.25", features = [ "v1_24" ] }
gst-base = { package = "gstreamer-base", version = "0.25", features = [ "v1_24" ] }
gst-plugin-rtp = { rev = "f8bdc4929ff79e2567d97c5e95ac90b452812c79", git = "https://gitlab.futo.org/fcast/gst-plugins-rs.git", default-features = false, features = [ "static", "depay" ] }
gst-plugin-webrtc = { rev = "f8bdc4929ff79e2567d97c5e95ac90b452812c79", git = "https://gitlab.futo.org/fcast/gst-plugins-rs.git", default-features = false }
gst-webrtc = { package = "gstreamer-webrtc", version = "0.25" }
gst-pbutils = { package = "gstreamer-pbutils", version = "0.25" }
gst-subparse = { git = "https://github.com/MAlba124/gst-subparse-rs.git", rev = "0cdf26b" }
# The cue-IR renderer stack (crates/fcast-video/src/cue_ir.rs). vello_cpu keeps
# the exact revision the upstream cueir-demo pins: the parley workspace's own,
# which its examples (and our rasterizer) are written against.
parley = "0.11"
peniko = "0.6"
vello_cpu = { git = "https://github.com/linebender/vello.git", rev = "5796226", default-features = false, features = [ "std", "png", "text", "u8_pipeline" ] }
serde_json = "1.0"
thiserror = "2.0"
log = "0.4"
env_logger = "0.11.8"
mdns-sd = "0.20.2"
anyhow = "1"
tracing = "0.1"
tracing-subscriber = "0.3"
tracing-gstreamer = { git = "https://github.com/MAlba124/tracing-gstreamer.git", rev = "3ad7a1e71a4ca4d1fd9a229642fd09586370acaf" }
tracing-panic = "0.1.2"
parking_lot = "0.12"
slint = { git = "https://gitlab.futo.org/fcast/slint.git", rev = "7e5f143307b251dd3afa51694e4063108ab38c44", default-features = false }
slint-build = { git = "https://gitlab.futo.org/fcast/slint.git", rev = "7e5f143307b251dd3afa51694e4063108ab38c44" }
i-slint-backend-winit = { git = "https://gitlab.futo.org/fcast/slint.git", rev = "7e5f143307b251dd3afa51694e4063108ab38c44" }
clap = { version = "4.5", features = ["derive"] }
uuid = { version = "1.23", features = [ "v4" ] }
bytes = "1"
smallvec = { version = "1", features = ["serde"] }
smol_str = { version = "0.3", features = ["serde"] }
toml_edit = { version = "0.25", features = ["serde"] }
futures = "0.3"
lazy_static = "1.5"
rand = "0.10"
num_cpus = "1"
image_swizzle = "0.3.0"
mimalloc = { version = "0.1", features = ["v3"] }
ctrlc = "3"
hyper = "1"
hyper-util = "0.1"
bindgen = "0.72"
webbrowser = "1"
glow = "0.17"
# Text shaping/rasterization for sink-side subtitle cues (fcast-video) and the
# static-build font closure (receiver-core).
pango = { version = "0.22.0", features = ["v1_44"] }
pangocairo = "0.22"
cairo-rs = "0.22"
libloading = "0.9.0"
askama = "0.15"
rustls = { version = "0.23", default-features = false }
tokio-rustls = { version = "0.26.4", default-features = false }
rustls-pki-types = "1.14"
sha2 = "0.11"
isolang = "2.4.0"
self_cell = "1.2.2"
directories = "6.0.0"
regex = "1.12"
infer = "0.19.0"
image = { version = "0.25", default-features = false }
base64 = "0.22"
x509-parser = "0.17"
rcgen = "0.14"

[profile.release]
lto = "fat"
codegen-units = 1
strip = "symbols"

[profile.release-dbg]
inherits = "release"
debug = "full"
strip = false

[profile.release-small]
inherits = "release"
opt-level = "s"
panic = "abort"

[profile.release-prof]
inherits = "release"
lto = "thin"
codegen-units = 16
debug = "line-tables-only"
strip = false

[profile.dev]
debug = "line-tables-only"
split-debuginfo = "unpacked"

[profile.dev.package]
read-fonts = { opt-level = 2 }
font-types = { opt-level = 2 }
skrifa = { opt-level = 2 }
harfrust = { opt-level = 2 }
parley = { opt-level = 2 }
fontique = { opt-level = 2 }
swash = { opt-level = 2 }
zeno = { opt-level = 2 }
icu_segmenter = { opt-level = 2 }
icu_properties = { opt-level = 2 }
zerotrie = { opt-level = 2 }
zerovec = { opt-level = 2 }
lyon_tessellation = { opt-level = 2 }
lyon_path = { opt-level = 2 }

[profile.dev.package.image]
opt-level = 3

[profile.dev.package.gif]
opt-level = 3
[profile.dev.package.weezl]
opt-level = 3
[profile.dev.package.png]
opt-level = 3
[profile.dev.package.fdeflate]
opt-level = 3
[profile.dev.package.miniz_oxide]
opt-level = 3
[profile.dev.package.image-webp]
opt-level = 3
[profile.dev.package.zune-jpeg]
opt-level = 3
[profile.dev.package.zune-core]
opt-level = 3
[profile.dev.package.color_quant]
opt-level = 3
[profile.dev.package.byteorder-lite]
opt-level = 3

[patch.crates-io]
# Disable encoders
image = { git = "https://gitlab.futo.org/fcast/image-rs.git", rev = "9ee78f3f" }
rustls = { git = "https://gitlab.futo.org/fcast/rustls.git", rev = "72ec207b" }
libheif-sys = { git = "https://gitlab.futo.org/fcast/libheif-sys.git", rev = "5f4f054de4cc7f64750c759b594df8462c0c86ce" }
